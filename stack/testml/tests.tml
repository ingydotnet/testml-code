%TestML: 1.0

%Plan: 3

empty.new_stack().pop_stack().Catch()
    == Str('Stack has no element to pop.');

init.List().new_stack()
    .push_stack('Hai!')
    .pop_stack() == Str('Hai!');

init.List().new_stack()
    .push_stack('yay')
    .head_stack() == Str('yay');

=== pop() empty stack makes error
--- empty

=== Test for stack operations
--- init
foo
bar
